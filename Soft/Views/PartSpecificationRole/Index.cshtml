@using HaSe.Pages.Controls
@using HaSe.Facade.Project
@model IEnumerable<HaSe.Facade.Project.PartSpecificationRoleViewModel>

@{
    ViewData["Description"] = "Part Specification Role";
    Layout = "_Index";
    var p = new ShowTableProperties<PartSpecificationRoleViewModel>(ViewBag);
    p.Items = Model;
}

@Html.ShowTable(p)


